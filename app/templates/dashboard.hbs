<div id="dashboard-container">
	{{dashboard-navbar openNewUploadModal="openNewUploadModal" closeNewUploadModal="closeNewUploadModal" setFilterType='setFilterType'}}
	{{component filterComponentName chooseClass="chooseClass" updateFilter="updateFilter"}}
	{{upload-list uploads=model}}
	{{#if showNewUploadModal}}
		{{#modal-dialog translucentOverlay=true close="closeNewUploadModal"}}
		{{/modal-dialog}}
	{{/if}}
</div>
